# Ansible playbook to build and deploy golang microservices for raspberry pi cluster

## Usage 

```bash
# cd to project directory 
$ ansible-playbook ansible-playbook  -i hosts.ini -u ubuntu playbook.yml
```
